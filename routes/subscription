const express = require("express");
const router = express.Router();
const  subscribeMonthly = require("../controllers/paymentController");

router.post("/monthly", subscribeMonthly);

module.exports = router,subscribeMonthly;